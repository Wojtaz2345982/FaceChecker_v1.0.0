@page
@model DeletePersonalDataModel
@{
    ViewData["Title"] = "Usuń dane osobowe";
    ViewData["ActivePage"] = ManageNavPages.PersonalData;
}

<h3 class="text-theme">@ViewData["Title"]</h3>

<div class="alert alert-warning" role="alert">
    <p>
        <strong>Usunięcie tych danych spowoduje trwałe usunięcie Twojego konta, którego nie będzie można odzyskać.</strong>
    </p>
</div>

<div>
    <form id="delete-user" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
        @if (Model.RequirePassword)
        {
            <div class="form-floating mb-3 input-box">
                <input asp-for="Input.Password" class="form-control bgColor bgColor inputFeatures text-theme" autocomplete="current-password" aria-required="true" placeholder="Wprowadź hasło." />
                <label asp-for="Input.Password" class="form-label text-theme"></label>
                <span asp-validation-for="Input.Password" class="text-danger"></span>
            </div>
        }
        <button class="w-100 btn btn-lg btn-danger" type="submit">Usuń dane i zamknij moje konto</button>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
